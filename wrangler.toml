name = "mpa-match"
main = "index.js"
compatibility_date = "2024-01-01"

# La API key se agrega como secreto desde el dashboard o con:
#   npx wrangler secret put ANTHROPIC_API_KEY
# Nunca la pongas aquí en texto plano.

[vars]
# Variables no sensibles van aquí (vacío por ahora)
